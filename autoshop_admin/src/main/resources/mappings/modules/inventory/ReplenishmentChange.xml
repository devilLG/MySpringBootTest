<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" ><mapper namespace="com.zhilai.master.modules.inventory.dao.ReplenishmentChangeDao" >		<sql id="Base_Column_List">		logid, change_id, order_id, corp_id, corp_name, 		site_id, site_name, oper_time, oper_id, oper_name, 		oper_action, create_time	</sql>		<select id="findList" resultType="ReplenishmentChange">		SELECT <include refid="Base_Column_List" /> FROM as_replenishment_change WHERE 1=1		<if test="orderId != null and orderId != ''">			AND order_id = #{orderId} 		</if>		<if test="beginTime != null and beginTime != ''">			AND create_time &gt;= #{beginTime} 		</if>		<if test="endTime != null and endTime != ''">			AND create_time &lt; ADDDATE(#{endTime},INTERVAL 1 DAY) 		</if>		ORDER BY create_time DESC 	</select>		<select id="get" resultType="ReplenishmentChange">		SELECT <include refid="Base_Column_List" /> 		FROM as_replenishment_change		WHERE logid = #{logid}	</select>		<delete id="delete">		DELETE FROM as_replenishment_change		WHERE logid = #{logid}	</delete>		<insert id="insert">		INSERT INTO as_replenishment_change(			logid, change_id, order_id, corp_id, corp_name,			site_id, site_name, oper_time, oper_id, oper_name,			oper_action, create_time )		VALUES(			#{logid}, #{changeId}, #{orderId}, #{corpId}, #{corpName},			#{siteId}, #{siteName}, #{operTime}, #{operId}, #{operName},			#{operAction}, #{createTime} )	</insert>		<update id="update">		UPDATE as_replenishment_change SET 			logid = #{logid},			change_id = #{changeId},			order_id = #{orderId},			corp_id = #{corpId},			corp_name = #{corpName},			site_id = #{siteId},			site_name = #{siteName},			oper_time = #{operTime},			oper_id = #{operId},			oper_name = #{operName},			oper_action = #{operAction},			create_time = #{createTime}		WHERE logid = #{logid}	</update></mapper>